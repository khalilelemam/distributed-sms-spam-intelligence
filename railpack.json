{
  "build": {
    "builder": "python",
    "apt_packages": ["default-jdk"],
    "install_command": "pip install -r fastapi_app/requirements.txt"
  },
  "run": {
    "command": "uvicorn fastapi_app.main:app --host 0.0.0.0 --port ${PORT:-8080}"
  }
}