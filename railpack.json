{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "build": {
    "aptPackages": ["default-jdk"]
  },
  "deploy": {
    "startCommand": "uvicorn fastapi_app.main:app --host 0.0.0.0 --port ${PORT:-8080}",
    "aptPackages": ["default-jre-headless"]
  }
}